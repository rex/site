Service = require './base'
config = require '../config'
async = require 'async'
_ = require '../lib/_'
logger = require '../lib/logger'

class Evernote extends Service

  constructor: ->

  fetch_recent_activity: (callback = ->) ->

  fetch_notebooks: (callback = ->) ->

  fetch_notes: (callback = ->) ->

  fetch_tags: (callback = ->) ->


module.exports = new Evernote