Service = require './base'
config = require '../config'
async = require 'async'
_ = require '../lib/_'
logger = require '../lib/logger'

class LinkedIn extends Service

  constructor: ->

  fetch_recent_activity: (callback = ->) ->

  fetch_profile: (callback = ->) ->

  fetch_connections: (callback = ->) ->

  fetch_user: (callback = ->) ->


module.exports = new LinkedIn